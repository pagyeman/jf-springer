$-- This partial is used to format the affiliations

\textsuperscript{\textbf{$it.number$}} $if(it.department)$$it.department$, $endif$$if(it.name)$$it.name$, $endif$$if(it.city)$$it.city$, $endif$$if(it.region)$$it.region$, $endif$$if(it.country)$$it.country$, $endif$
