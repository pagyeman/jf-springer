$-- This partial is used to format author name, including orcid-link and degrees.
$-- Degrees is a non-standard field in the normalised quarto author scheme.
$-- This '_custom.tex' is used on 'title.tex' as example.

$it.name.literal$$if(it.orcid)$\orcidlink{$it.orcid$}$endif$$if(it.metadata.degree)$~$it.metadata.degree$$endif$\textsuperscript{$for(it.affiliations)$$it.number$$sep$,$endfor$}$if(it.attributes.equal-contributor)$\Yinyang$endif$$if(it.attributes.corresponding)$*$endif$
